// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyCnZsAhJ0YuDsvoaYWA62jZH121ROO8u9c",
    authDomain: "wippycatherine.firebaseapp.com",
    databaseURL: "https://wippycatherine.firebaseio.com",
    projectId: "wippycatherine",
    storageBucket: "wippycatherine.appspot.com",
    messagingSenderId: "700698664500",
    appId: "1:700698664500:web:96e4e480968bb27c"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
